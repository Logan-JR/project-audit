import Gallery from "@/ui/cpa/gallery/gallery";

const GalleryPage = ({ searchParams }) => {
  return (
    <>
      <Gallery searchParams={searchParams} />
    </>
  );
};

export default GalleryPage;
